# Seed default profiles

## Goal
Load the baseline system profiles used by ZenOps.

## Deliverables
- Run the profiles section of `seed/seed.sql`

## Dependencies
- profiles table exists

## Implementation Notes
- System profiles use `organization_id = null`.

## Acceptance Criteria
- Required system profiles exist
